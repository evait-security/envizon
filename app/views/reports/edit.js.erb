$("#medium-modal-content").html("<%= j render(partial: 'reports/edit') %>");
$(".report-editor").each(function(index, value) {
    ClassicEditor
    .create( value, {
        toolbar: {
            items: [
                'bold',
                'italic',
                'fontColor',
                'fontBackgroundColor',
                'codeBlock',
                'highlight',
                'fontFamily',
                'horizontalLine',
                'specialCharacters',
                'link',
                'undo',
                'redo'
            ]
        },
        language: 'de'				
    });
});
M.updateTextFields();
$("#medium-modal").modal('open');