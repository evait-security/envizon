general_modal = bootstrap.Modal.getInstance(document.getElementById('general-modal'))
if (general_modal) { general_modal.hide(); }

toastr.options.positionClass = "toast-bottom-right";
toastr.success('Note has been created successfully');

<% if @noteable.class.name == "Client" %>
  $.ajax({
    url: "/clients/<%= @noteable.id %>",
    dataType : "script",
    cache: true,
    method: "get"
  });
<% end %>
