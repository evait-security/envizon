$("#general-modal-content").html("<%= j(render('delete_clients', :clients => clients, :source_group => source_group, :search => search)) %>");
new bootstrap.Modal(document.getElementById('general-modal')).show();
