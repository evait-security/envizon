$("#medium-modal").html("<%= j(render('move', :clients=> clients, :source_group=> source_group, :search=> search)) %>");
$("#medium-modal").modal('open');
$('select').formSelect();
M.updateTextFields();
